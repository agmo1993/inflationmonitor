# Clerk (https://clerk.com) — required for auth in normal / production modes
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx
CLERK_SECRET_KEY=sk_test_xxx
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-in

# ---------------------------------------------------------------------------
# AUTH_DEV_BYPASS — LOCAL DEVELOPMENT ONLY
# Set to "1" to skip Clerk and authenticate as fixed account_id "dev_bypass_user".
#
# Bypass is active ONLY when ALL of the following hold (checked at call time):
#   - AUTH_DEV_BYPASS=1
#   - NODE_ENV=development
#   - NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY and CLERK_SECRET_KEY are both unset or empty
#   - VERCEL is not "1"
#   - VERCEL_ENV is not "production"
#
# Safety: either Clerk key alone (non-empty) disables bypass.
# Without the allow flag below, Vercel hosting and production refuse bypass
# (VERCEL=1 or VERCEL_ENV=production / NODE_ENV=production).
# Never enable on shared long-term production environments.
# ---------------------------------------------------------------------------
# AUTH_DEV_BYPASS=1
# Optional client hint for UI copy (does not enable bypass by itself):
# NEXT_PUBLIC_AUTH_DEV_BYPASS=1

# ---------------------------------------------------------------------------
# AUTH_ALLOW_VERCEL_BYPASS — TEMPORARY / testing-only
# Escape hatch for intentional Vercel preview / test deploys where Next always
# sets NODE_ENV=production and VERCEL=1 (so AUTH_DEV_BYPASS alone cannot work).
#
# When AUTH_DEV_BYPASS=1 AND AUTH_ALLOW_VERCEL_BYPASS=1 AND both Clerk keys are
# empty, bypass is ON even on Vercel / production NODE_ENV.
# Does nothing without AUTH_DEV_BYPASS=1. Clerk keys still disable bypass.
# Do NOT leave enabled on a real public production deployment long-term.
# ---------------------------------------------------------------------------
# AUTH_ALLOW_VERCEL_BYPASS=1

# Cloudflare Workers AI — required chat LLM
CLOUDFLARE_ACCOUNT_ID=your_account_id
CLOUDFLARE_API_TOKEN=your_api_token
# Optional model override (default: @cf/zai-org/glm-4.7-flash)
# CHAT_MODEL=@cf/zai-org/glm-4.7-flash
# Optional metering cost proxy overrides (USD):
# CF_COST_INPUT_PER_MTOK=0.30
# CF_COST_OUTPUT_PER_MTOK=0.50
# CF_COST_PER_REQUEST_USD=0.002

# Postgres / Neon — app usage metering (separate from CPI tables in packages/data)
# Uses table app_usage_monthly (namespaced; does not touch CPI schema)
DATABASE_URL=postgres://user:pass@host/dbname?sslmode=require

# Public app URL (Vercel)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Vercel monorepo: set Project Root Directory to apps/web
